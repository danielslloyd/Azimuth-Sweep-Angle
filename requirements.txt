# Project Overwatch - Python dependencies

fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0

# Optional: local Whisper STT (only needed for the "Whisper (server)" STT mode)
# pip install faster-whisper
# faster-whisper>=1.0.0
