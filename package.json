{
  "name": "project-overwatch",
  "version": "2.0.0",
  "description": "Voice-controlled tactical squad game with local LLM command parsing",
  "scripts": {
    "start": "python -m uvicorn server.main:app --host 127.0.0.1 --port 8000"
  }
}
